const dimensions = { left: -11, right: 89, top: 0, bottom: 85 }
const shotRadius = 2
const transitionDuration = 1500
const transitionDelay = 1000

export {
	dimensions,
	shotRadius,
	transitionDuration,
	transitionDelay,
}
