const dimensions = {
	left: -25,
	right: 25,
	top: -5.25,
	bottom: 41.75,
}

const binRatio = 1 / 35
const radiusRangeFactors = [0.25, 0.5, 0.75, 1]

const percentRange = 12

const colorClasses = ['below', 'average', 'above']

export {
	dimensions,
	binRatio,
	radiusRangeFactors,
	percentRange,
	colorClasses,
}
